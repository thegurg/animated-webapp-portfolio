import 'package:flutter_test/flutter_test.dart';

void main() {
  test('dummy test — чтобы CI не падал пока нет реальных тестов', () {
    expect(true, isTrue);   // или просто expect(1 + 1, 2);
  });
}